{
  "name": "safora",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  },
  "scripts": {
    "prepare": "npx husky"
  },
   "lint-staged": {
    "apps/backend/**/*.ts": ["eslint --fix", "prettier --write"],
    "apps/mobile/**/*.{ts,tsx}": ["eslint --fix", "prettier --write"]
  }
}
